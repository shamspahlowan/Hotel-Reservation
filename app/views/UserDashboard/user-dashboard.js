function navigate(url) {
    window.location.href = url;
  }
  
  function logout() {
    // Clear session or cookies as needed
    alert("Logging out...");
    window.location.href = "login.html"; // Or handle via PHP logout script
  }
  